[project]
name = "Rhytmer"
description = "Rhytmer is a TUI audio downloader based on yt-dlp"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yt-dlp",
    "textual",
    "fake_useragent",
    "mutagen"
]

[project.optional-dependencies]
dev = [
    "ruff",
]