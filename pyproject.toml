[project]
name = "Rhythmer"
description = "Rhythmer is a TUI audio downloader based on yt-dlp"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "yt-dlp",
    "textual",
    "mutagen"
]

[project.optional-dependencies]
dev = [
    "ruff",
]